name: imgstore

channels:
  - conda-forge
  - defaults

dependencies:

  - python

  - numpy
  - pandas
  - bloscpack

  - opencv
  - pims

  - ruamel.yaml

  - ipython
  - jupyter

  - pytest
  - pytest-cov
  - pytest-pep8

  - pip:
    - "--editable=./"
